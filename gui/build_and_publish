#!/bin/sh

rm -rf .rev \
&& rm -rf .publish_stage \
&& mkdir .publish_stage \
&& gulp build \
&& cd .publish_stage \
&& git init \
&& git checkout -b gh-pages \
&& git remote add origin git@github.com:blakelapierre/game \
&& git pull origin gh-pages \
&& git rm -r . \
&& cp  -r ../.rev/* . \
&& git add . \
&& git commit -m ".rev" \
&& git push origin gh-pages
